# 介面與操作流程檢查

## 修改前狀態

基準提交：`0238174`；工作分支：`agent/optimize-gameplay-core`。開始時工作目錄乾淨。

專案使用 Vite、Three.js 與原生 HTML/CSS/JavaScript，已有 3 張可玩地圖、5 種模式、20 把武器、每日挑戰、自訂規則、本機統計與成就。現有單元及 Playwright 測試可沿用，不需要更換前端框架。

原首頁使用左側長列表，1280×720 下次要入口落在畫面下方、右側空間未利用。設定與統計面板缺少通用高度限制；小字、鍵盤焦點及面板切換是主要問題。修改前已成功建置並執行既有視覺擷取測試。

## 本次處理

| 問題或不足 | 修改 |
| --- | --- |
| 出戰設定及次要入口混在長列表 | 雙欄戰備室；窄螢幕單欄，依出戰、簡報、整備分組 |
| 模式勝利條件不清楚 | 即時簡報顯示模式目標、地圖、難度與規則；Hunt 說明對齊實際 3 波條件 |
| 無法直接產生新種子 | 新增隨機種子操作及重玩說明 |
| 開啟 Challenges 會覆寫設定 | 改成唯讀預覽，另提供明確套用按鈕 |
| 每日挑戰缺少取消操作 | 可恢復套用前的種子、地圖、模式、難度及自訂開關；重複套用不覆寫原設定 |
| 選 Custom modifier 可能完全不生效 | 自動啟用並展開自訂規則；簡報顯示有效規則 |
| 武器庫只能逐項瀏覽 | 新增名稱、類別及配件搜尋，顯示結果數與無結果提示 |
| 面板背景仍可操作，缺少焦點管理 | 統一面板開關、dialog 名稱、焦點循環、Escape 返回及觸發按鈕焦點還原 |
| 設定面板太高，手機底部操作不可達 | 所有面板限制動態視窗高度並允許捲動；手機改單欄 |
| 主選單設定露出無關戰鬥 HUD | HUD 僅在戰鬥階段顯示 |
| 種子數字觸發換槍，選單輸入累積遊戲按鍵 | 戰鬥輸入限制在 playing 階段；切換階段清除按住與瞄準狀態 |
| 主選單缺少完整操作指南入口 | 新增指南按鈕，補上 E 互動提示 |
| 自訂數值清空被當成零後套用下限 | 空值改回該欄位預設值 |
| 瀏覽器停用 storage 可能阻止啟動 | 安全取得 storage，顯示儲存失敗提示並維持可遊玩 |
| 舊存檔遷移寫入失敗使已讀進度變空白 | 仍回傳成功讀取的舊進度，保留原存檔 |
| 結算可重入，勝敗畫面與存檔不一致 | 已結算後忽略後續結算要求 |
| 每日挑戰最後一發命中在儲存後才計入 | 等同步戰鬥事件完成後結算，保存最後一發統計 |
| 暫停時間被記入遊玩時間 | 僅累計 playing 階段的經過時間 |
| 載入完成後一般錯誤被錯標為核心載入失敗 | 核心載入完成時移除初始化錯誤監聽 |
| 文字太小、焦點不清楚 | 共用顏色、字級與焦點樣式，尊重減少動態效果偏好 |

## 驗證範圍

使用專案既有的 lint、TypeScript、Node 測試、正式建置、資產檢查與 Chromium Playwright 流程。新增介面及結算回歸案例；桌面 1280×720、手機 390×844 檢查溢出與面板可達性，沿用遊戲地圖、戰鬥、設定、結算視覺擷取。

最終結果：lint、typecheck、build、assets:check 均通過；單元測試 19/19，完整瀏覽器測試 19/19（`npm run test:e2e -- --workers=2`）。新增 1 項單元及 12 項瀏覽器案例。桌面出戰按鈕可在 1280×720 首屏完整顯示；390×844 設定底部返回按鈕可透過面板捲動操作。

最後交叉檢查補測「達標後、結算前切換非終局階段」：先重現勝利畫面未出現，再將延後結算條件改為尚未結算，重建後結算回歸測試 2/2 通過。這避免正常暫停或升級階段略過每日勝利，同時保留死亡結算優先。

修改檔案：`index.html`、`src/style.css`、`src/main.js`、`src/core/SaveManager.ts`、`test/rules.test.js`；新增 `e2e/interface.spec.ts`、`e2e/settlement.spec.ts`、`DESIGN.md` 與本報告。無新增依賴、無遠端寫入。

## 實際限制

- 響應式選單不等於觸控 FPS；目前戰鬥仍使用鍵盤、滑鼠及 Pointer Lock，未新增觸控控制器。
- Frozen、Desert、Neon 為標示不可用的地圖資料，並非本次新增的可玩內容。
- 進度仍為本機儲存，沒有雲端帳號、跨裝置同步或排行榜。
- 自動化瀏覽器驗證使用 Chromium；未宣稱已驗證所有瀏覽器、實體 GPU、輸入設備或全部種子組合。
- 此紀錄列出本次有證據且已處理的問題，不代表遊戲不存在其他缺陷。
